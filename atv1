#include <stdio.h>
#include <stdlib.h>

int main() {
	int numero, resto;
	printf("Digite um numero: ");
	scanf("%d", &numero);
	
	resto = numero % 2;
	
	if (resto == 0){
		printf("%d O numero é par.\n", numero);
	}else{
		printf("%d O numero é impar.\n", numero);
	}
	
	return 0;
}
